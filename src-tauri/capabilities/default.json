{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main PipelineSync window. The app is a self-contained SPA: it needs no filesystem, shell or network plugins beyond the default window/event APIs.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default", "core:window:default"]
}
